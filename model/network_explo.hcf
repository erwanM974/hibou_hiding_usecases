@explore_option{
    loggers = [tracegen[terminal,
                        partition=discrete]];
    strategy = HCS;
    priorities = random;
    filters = [ max_loop_depth = 10, max_node_number = 20000 ]
}